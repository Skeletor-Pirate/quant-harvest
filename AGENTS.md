# Master Agent Instructions & Skill Suite

Every CLI Agent (Claude Code, Hermes Agent, and OpenCode) operating in this workspace MUST follow these skills:

---

## 🚀 1. Fullstack & UI Craft Skills
- **`impeccable-design`**: Anti-AI-slop design system. Curated HSL color harmony, glassmorphism (`backdrop-filter`), Google Fonts (Inter, Outfit), and dynamic layout math.
- **`emil-design-eng`**: Emil Kowalski UI craft & micro-interactions.
- **`emil-animate`**: High-performance spring physics (`stiffness: 300, damping: 30`) and GPU-accelerated CSS transitions.
- **`nextjs-fullstack-craft`**: Next.js 15 App Router, Server Actions, type-safe Zod validation, and dynamic route configuration.

---

## ⚡ 2. Token Savings & Context Intelligence
- **`graphify`**: AST dependency graph parsing. Queries structural code maps instead of reading massive files line-by-line.
- **`headroom`**: Context compression & log payload minimization.

---

## 🔒 3. Backend, Security & State Architecture
- **`database-architect`**: Prisma / Drizzle ORM schemas, compound indexes, N+1 query elimination, and strict migration history.
- **`api-security-hardener`**: OWASP Top 10 mitigation, rate limiting (`X-RateLimit-*`), `httpOnly` secure cookies, and input sanitization.
- **`state-management-master`**: TanStack Query (React Query) for server state caching + Zustand for transient client state.

---

## 🧪 4. Testing & Cloud Deployment
- **`playwright-testing-pro`**: End-to-end user-visible locators (`getByRole`), auto-waiting assertions, and trace logging.
- **`docker-cloud-deploy`**: Multi-stage Dockerfiles, Docker Compose service orchestration, Nginx reverse proxy, and SSL termination.

---

## 🛠️ 5. GStack Product & Release Workflows
- **`/office-hours`**: Product strategy, requirements framing, and assumption testing.
- **`/review`**: Code review, security auditing, and vulnerability checks.
- **`/ship`**: Production build verification and git release.
